/* eslint-disable prefer-arrow/prefer-arrow-functions */
import { IContextState, initialContextState } from '../state/context.state';

export function contextReducer(state = initialContextState): IContextState {
  return state;
}
